[project]
name = "AvroraParser"
version = "1.0.0"
description = "Parser for Avrora with love and AI by @entaytion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "aiohttp",
    "psycopg2-binary",
    "python-dotenv",
    "pillow",
    "selectolax",
]
